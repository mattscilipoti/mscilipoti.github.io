# mscilipoti.github.io
All about me

## Running the Jekyll Server

To serve the Jekyll site locally, use the following command:

```
bundle exec jekyll serve
```

This ensures that the `jekyll` command is run within the context of the gems specified in the `Gemfile`.
